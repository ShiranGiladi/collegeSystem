import React from 'react';
import { Navbar } from 'react-bootstrap';

const Placeholder = () => {
  return (
    <Navbar>
        <Navbar.Brand >
        </Navbar.Brand>
    </Navbar>
  );
};

export default Placeholder;
